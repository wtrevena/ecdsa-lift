.venv/
__pycache__/
*.pyc
.DS_Store

# Result JSON is part of the artifact and IS committed (the paper footnote
# promises "code, data, and a per-phase index"). Do not ignore results/.

# Cowork scratchpad (not part of the repo)
outputs/
